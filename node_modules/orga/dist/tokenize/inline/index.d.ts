import { Reader } from 'text-kit';
import { Tokenizer } from '../index.js';
import { Token } from '../../types.js';
export declare const tokenize: (reader: Reader, tokenizers?: Tokenizer[], { ignoring }?: {
    ignoring: string[];
}) => Token[];
