import { Reader } from 'text-kit';
import { Point } from 'unist';
import { Token } from '../../types';
declare const tokenizeText: (bol?: Point | undefined) => (reader: Reader) => Token[];
export default tokenizeText;
