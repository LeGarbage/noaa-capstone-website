import { Reader } from 'text-kit';
import { Token } from '../types.js';
declare const _default: ({ keywords, timezone, }: {
    keywords: string[];
    timezone: string;
}) => (reader: Reader) => Token[] | void;
export default _default;
