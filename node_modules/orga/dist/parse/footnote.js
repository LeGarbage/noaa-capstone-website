const Footnote = (token, { enter, exitTo, consume }) => {
    exitTo('document');
    enter({
        type: 'footnote',
        label: token.label,
        children: [],
    });
    consume();
};
export default Footnote;
