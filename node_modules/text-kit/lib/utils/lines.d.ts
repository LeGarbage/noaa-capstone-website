/**
 * @import {Point,Position} from 'unist'
 * @import {CoreAPI} from '../core.js'
 */
/**
 * @callback linePosition
 * @param {Point|number} start
 * @param {Point|number} [end]
 * @returns {Position|null}
 *
 * @callback endOfLine
 * @param {Point|number} ln
 * @returns {Point|undefined}
 *
 * @callback beginOfLine
 * @param {Point|number} ln
 * @returns {Point|undefined}
 *
 */
/**
 * @template {CoreAPI} T
 * @param {T} core
 * @returns {T & {linePosition: linePosition, endOfLine: endOfLine, beginOfLine: beginOfLine}}
 */
export default function _default<T extends CoreAPI>(core: T): T & {
    linePosition: linePosition;
    endOfLine: endOfLine;
    beginOfLine: beginOfLine;
};
export type linePosition = (start: Point | number, end?: number | Point | undefined) => Position | null;
export type endOfLine = (ln: Point | number) => Point | undefined;
export type beginOfLine = (ln: Point | number) => Point | undefined;
import type { CoreAPI } from '../core.js';
import type { Point } from 'unist';
import type { Position } from 'unist';
//# sourceMappingURL=lines.d.ts.map