/**
 * @import {Point} from 'unist'
 * @import {CoreAPI} from '../core.js'
 */
/**
 * @callback substring
 * @param {Point|number} start
 * @param {Point|number} [end]
 * @returns {string}
 */
/**
 * @template {CoreAPI} T
 * @param {T} core
 * @returns {T & {substring: substring}}
 */
export default function addSubstring<T extends CoreAPI>(core: T): T & {
    substring: substring;
};
export type substring = (start: Point | number, end?: number | Point | undefined) => string;
import type { CoreAPI } from '../core.js';
import type { Point } from 'unist';
//# sourceMappingURL=substring.d.ts.map