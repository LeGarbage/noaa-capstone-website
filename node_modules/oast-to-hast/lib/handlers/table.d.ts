/**
 * @import {Element,ElementContent} from 'hast'
 * @import {Table,TableCell,TableRow} from 'orga'
 * @import {State} from '../state.js'
 */
/**
 * @param {State} state
 * @param {Table} node
 * @returns {Element}
 */
export function table(state: State, node: Table): Element;
/**
 * @param {State} state
 * @param {TableRow} node
 * @returns {Element}
 */
export function row(state: State, node: TableRow): Element;
/**
 * @param {State} state
 * @param {TableCell} node
 * @returns {Element}
 */
export function cell(state: State, node: TableCell): Element;
/**
 * @returns {Element}
 */
export function hr(): Element;
import type { State } from '../state.js';
import type { Table } from 'orga';
import type { Element } from 'hast';
import type { TableRow } from 'orga';
import type { TableCell } from 'orga';
//# sourceMappingURL=table.d.ts.map